# THIS FILE WAS AUTOMATICALLY GENERATED, PLEASE DO NOT EDIT.
#
# Generated on 2024-03-14T09:48:15Z by kres latest.

env:
  USERNAME: ${{ github.repository_owner }}

name: default
concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
"on":
  push:
    branches:
      - main
      - release-*
    tags:
      - v*
  pull_request:
    branches:
      - main
      - release-*
jobs:
  default:
    permissions:
      actions: read
      contents: write
      issues: read
      packages: write
      pull-requests: read
      id-token: write
    runs-on:
      - ubuntu-latest
    if: (!startsWith(github.head_ref, 'renovate/') && !startsWith(github.head_ref, 'dependabot/'))
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: Unshallow
        run: |
          git fetch --prune --unshallow
      - name: Set up Namespace CLI
        uses: namespacelabs/nscloud-setup@v0
      - name: Set up Docker Buildx
        uses: namespacelabs/nscloud-setup-buildx-action@v0
      - name: Build
        run: |
          make
      - name: Login to registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
          username: ${{ github.repository_owner }}
      - name: Push to registry
        if: github.event_name != 'pull_request'
        run: |
          make PUSH=true TAG=v1.7.0
      - name: release-notes
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          make release-notes
      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: crazy-max/ghaction-github-release@v2
        with:
          body_path: _out/RELEASE_NOTES.md
          draft: "true"
      - name: Build soquartz-cm4 image
        run: |
          docker run --rm -t -v $PWD/_out:/out -v /dev:/dev --privileged ghcr.io/$USERNAME/imager:v1.7.0 metal --arch arm64 \
          --overlay-name soquartz-cm4 --overlay-image ghcr.io/$USERNAME/sbc-rockchip:v1.7.0 \
          --system-extension-image ghcr.io/siderolabs/iscsi-tools:v0.1.4 \
          --system-extension-image ghcr.io/siderolabs/util-linux-tools:2.39.3
      - name: Build turing-rk1 image
        run: |
          docker run --rm -t -v $PWD/_out:/out -v /dev:/dev --privileged ghcr.io/siderolabs/imager:v1.7.0-beta.1 metal --arch arm64 \
          --overlay-name turing-rk1 --overlay-image ghcr.io/$USERNAME/sbc-rockchip:v1.7.0 \
          --system-extension-image ghcr.io/siderolabs/iscsi-tools:v0.1.4 \
          --system-extension-image ghcr.io/siderolabs/util-linux-tools:2.39.3
      - name: Upload images
        uses: actions/upload-artifact@v2
        with:
          name: images
          path: _out
